# 领域约定

青年健康分层随访引擎围绕时间化健康资料、风险规则与随访任务保存业务事实。主要对象为组织、资料版本、班次区间、规则版本、分层快照、随访任务、访问授权。

必须长期遵守以下约束：分层结果固定输入和规则版本；高等级任务只能抢占未执行任务；授权撤回冻结未来使用；紧急访问到期后进入复核。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。